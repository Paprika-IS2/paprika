<%= form_for(type_dish) do |f| %>
  <% if type_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_dish.errors.count, "error") %> prohibited this type_dish from being saved:</h2>

      <ul>
      <% type_dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
